# Release checklist

- Replace the placeholder citation in README.md.
- Add the external download link for `best.pt`.
- Test inference once from a clean environment.
- Run `git status` and confirm videos/checkpoints are not staged.
- Create a release tag such as `v1.0.0`.
